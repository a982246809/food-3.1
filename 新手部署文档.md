# 餐饮管理系统 (food-3.1) - Docker 一键部署指南 🚀

欢迎加入本项目！这是一份由于新手准备的基于 Docker 的全量级一键部署指南。通过这种方式，你可以用最简便的操作将包含数据库、后端 API 以及所有前端页面的完整系统完整运行起来。并且所有的环境均不需要本地配置。

## 🎯 基础运行环境准备
在开始任何操作之前，请确保你的电脑已经安装了必备的核心引擎：

1. **Docker Desktop**
   - 从 [Docker 官网](https://www.docker.com/products/docker-desktop) 下载并安装。
   - 安装完成后，请**必须启动该程序**（确保 Windows 任务栏系统托盘右下角里面有一个小鲸鱼 🐳 的图标，并处于绿色的运行中状态）。
2. **Git**
   - 从 [Git 官网](https://git-scm.com/) 下载并安装代码版本控制工具（拉取项目源代码必备）。

---

## 🐳 一键部署启动

本项目预先配置了完善的容器化网络环境，基于 `docker-compose` 来编排和集成：MySQL 数据库、NestJS 后端应用容器、以及被 Nginx 反向代理包裹的所有前端 Vue 应用。

### 第 1 步：执行自动部署命令
在你的**项目根目录**（包含了 `docker-compose.yml` 文件的这个主文件夹，例如 `d:\Study\food-3.1`）下，右键打开终端或 PowerShell，然后执行：

```bash
docker-compose up -d --build
```

**☕ 请耐心等待 3~10 分钟：**
因为 Docker 容器引擎需要在系统后台为你自动且一次性地拉取基础镜像资源、下载所有前后端 node\_modules 依赖包、构建后端并完整将前端编译打包成为最终用来被 Nginx 代理解析的静态资源，它所需的时间较长。全部完成后它会自动拉起所有容器实例进程。

### 第 2 步：访问部署好的系统入口
看到终端输出一排排绿色的 `Started` 并完成后，请在你的浏览器中直接访问以下地址（**全部通过本机的 80 端口即可访问，Nginx 内部代理已经做了对应的路由和端口映射去除了例如 5173、3000 之类的端口尾巴**）：

* 📱 **学生端 (点单大厅前台页面):** [http://localhost](http://localhost)
* 👑 **系统管控超级管理后台:** [http://localhost/admin/](http://localhost/admin/)
* 🏬 **餐饮商家专享管理后台:** [http://localhost/merchant/](http://localhost/merchant/)
* 🔌 **供前端或测试访问用的后端 API 接口起点:** [http://localhost/api/](http://localhost/api/)

---

## 🛑 管理你的容器实例

### 如何停止项目？
当你今天测试完毕了，或者想要释放相关的电脑系统资源时，同样在你执行启动所在的目录开启终端页面输入以正常中止全部项目：
```bash
docker-compose down
```

### 数据库内容我想全部清空回到初始化原始状态呢？
在执行下线指令的后面附加清理卷的控制参数：
```bash
docker-compose down -v
```
再次通过 `docker-compose up -d --build` 重新构建并冷启动时。它又会变成一套干爽纯净的新系统。（包括数据库内的自动填入的数据都将从头来过）。

---

## ❓ 常见的新手防坑排查方案 (FAQ)

### ❓ 1. 为什么在控制台执行 `docker-compose up` 报错连不上容器守护进程 (Daemon)？
因为你还没开始运行 Docker。很多新同学容易忘记打开 `Docker Desktop` 桌面端应用就开始敲代码。

### ❓ 2. 用 Docker 跑起来后，系统默认的登录账号与密码是什么？
系统在首次运行时，会自动将内置的假测试数据注入数据库，包含管理员、商户和学生账号，**所有账号的默认密码均为 `123456`**：

* 👑 **系统管理员账号：** `admin` (可用在系统管控超级管理后台)
* 🏬 **餐饮商家账号：** `merchant`、`merchant2`、`merchant3`、`merchant4`、`merchant5` (可用在餐饮商家专享管理后台)
* 📱 **学生点餐账号：** `student`、`student2`、`student3`、`student4` (可用在学生端前台)

如果想深入了解测试数据的详情或是添加新的预设数据，您可以直接查看后端初始化脚步：`server/prisma/seed.ts`。
